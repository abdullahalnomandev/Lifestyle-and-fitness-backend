export enum USER_AUTH_PROVIDER {
  LOCAL = 'local',
  GOOGLE = 'google',
  MOBILE = 'mobile',
}

export enum PROFILE_MODE {
    PRIVATE = 'private',
    PUBLIC = 'public'
}
export const userSearchableField = ['name', 'email'];
